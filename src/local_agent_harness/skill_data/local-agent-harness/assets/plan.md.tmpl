# Session Plan — `<task-slug>`

> Ephemeral but auditable. Update before edits and at every decision.

## Inputs
- Task statement: `<one sentence>`
- Linked issue / PR: `<url or n/a>`
- Maturity stage at session start: `<S0 | S1 | S2 | S3>`

## Non-goals
- `<explicitly out-of-scope item>`

## Allowed scope
- Files: `<paths>`
- Commands: `<allowlisted commands>`
- Network: `<denied | allowlist subset>`

## Proposed steps
1. `<step>`
2. `<step>`

## DryRUN predictions
- Files to touch: `<paths>`
- Tests to run: `<commands>`
- Expected diff size: `<small | medium | large>`
- Expected risk: `<low | medium | high>` and why.

## Verification
- [ ] Lint
- [ ] Tests
- [ ] Secrets scan
- [ ] SAST / deps (if S2+)
- [ ] Maintainability gate (if S2+)
- [ ] Manifest regression (if manifests changed)

## Decisions log
- `<timestamp>` — `<decision>` — `<reason>`
