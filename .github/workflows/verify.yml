name: verify
on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup language toolchain
        run: echo "TODO: configure for this repo"
      - name: Lint / format
        run: echo "TODO: run linter"
      - name: Unit tests
        run: echo "TODO: run tests"
      - name: Secrets scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Manifest regression (if scripts/manifest_regression.py exists)
        run: |
          if [ -f scripts/manifest_regression.py ]; then
            python scripts/manifest_regression.py
          fi
